---
interface Props {
  pageTitle: string;
}

import { MainLayout } from '@blackbyte/website-common';
import '../components/index.css';
import __websiteConfig from '../../website-sugar.config';
import __packageJson from '../../package.json';
---

<script>
  import '../components/index';
</script>

<MainLayout
  menu={__websiteConfig.menu}
  version={__packageJson.version}
  og={__websiteConfig.og}
  license={__websiteConfig.license}
  repository={__websiteConfig.repository}
  title={__websiteConfig.title}
  pageTitle={Astro.props.pageTitle}
  description={__websiteConfig.description}
  menu={__websiteConfig.menu}
>
  <slot />
</MainLayout>
