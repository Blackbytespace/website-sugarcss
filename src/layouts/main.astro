---
interface Props {
  pageTitle: string;
}

import { MainLayout, type TWebsiteConfig } from '@blackbyte/website-common';
import '../components/index.css';
import websiteConfig from '../../website.config';
---

<script>
  import '../components/index';
</script>

<MainLayout pageTitle={Astro.props.pageTitle} websiteConfig={websiteConfig}>
  <slot />
</MainLayout>
